# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem 'rspec'
gem 'rubocop', '~> 1.22', require: false
gem 'simplecov', require: false, group: :tesgem
gem 'simplecov-console'
